import { migrateCollection } from '../utils.js';

// This is the master list of all pre-loaded collections available in the catalog.
// This is now populated dynamically from a backend API call in CatalogScreen.js
// To add a new default collection, you must now use the admin seeding endpoint.

export const ALL_COLLECTIONS = [];
